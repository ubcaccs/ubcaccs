{% extends "_layouts/main" %}


{% block main %}


{# Home Section #}
<div class="flex justify-around flex-wrap items-start pt-8 flex-grow">
    {# Voice Text #}
    <div class="align-start pm-5 pb-28 pt-16 pr-2">
        <h1 class="text-off-white text-6xl font-bold text-left mb-4 font-headerFont max-w-[550px] leading-normal pb-2">
            {{ entry.title }}
        </h1>
        <p class="text-off-white text-[20px] text-left font-mainFont max-w-[550px]">{{ entry.subtext}}</p>
    </div>
    
    <img class="logo-image w-[650px] pr-4" src="{{ siteUrl }}/images/static/microphone.png" alt="Microphone">
</div>

{# Event Section #}
<div class="h-svh">
    <h1 class="text-off-white">
        Events
    </h1>
</div>

{# About Section #}
<div class="h-svh">
    <h1 class="text-off-white">
        About
    </h1>
</div>

{# Contact us Section #}
<div class="h-svh">
    <h1 class="text-off-white">
        Contact us
    </h1>
</div>
{% endblock %}
